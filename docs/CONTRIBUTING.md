# Contributing Guidelines

## Code Contribution Workflow
1. **Branch Out**: Create a branch from `main` matching format: `feature/name` or `bugfix/name`.
2. **Coding Standards**: Ensure linting checks pass cleanly.
3. **Commit**: Save clean commits with conventional names.
4. **Pull Requests**: Submit PRs to `main` with detailed descriptions of modifications.
